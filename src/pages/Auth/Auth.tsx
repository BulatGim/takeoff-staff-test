import {FC} from 'react';
import AuthForm from "../../molecules/AuthForm/AuthForm";

const Auth: FC = () => {
    return (
        <div>
            <AuthForm/>
        </div>
    );
};

export default Auth;